import { create_renter_validation } from "./create_renter.validation";
import { update_renter_validation } from './update_renter.validation';

export default {
  create_renter_validation,
  update_renter_validation
}